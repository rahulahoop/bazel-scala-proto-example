# Damn Simple Scala Proto Bazel Binary

### Run it!
> $ bazel build //src/example:main \
> $ bazel run //src/example:main

Just messing around with bazel to bundle a scala binary and include 
generated protobuf code 💪🏽

